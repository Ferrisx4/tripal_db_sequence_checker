# Schema for the configuration files of the Tripal DB Sequence Checker module.
tripal_db_sequence_checker.settings:
  type: config_object
  label: 'Tripal DB Sequence Checker settings'
  mapping:
    example:
      type: string
      label: 'Example'